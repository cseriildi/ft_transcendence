# Frontend - Local Development Environment
# This file is used to inject runtime configuration into the frontend

# Server configuration (for browser-sync dev server)
PORT=4200
NODE_ENV=development

# API URLs (injected into index.html at build time via entrypoint.sh)
# For local dev without NGINX proxy, services run on localhost
PUBLIC_API_URL=http://localhost:3000/api
PUBLIC_WS_URL=ws://localhost:3002/ws

# Logging (for build tools)
LOG_LEVEL=debug
