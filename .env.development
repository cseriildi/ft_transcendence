# =============================================================================
# Development Environment Configuration - REFERENCE ONLY
# =============================================================================
# ⚠️ NOTE: This file is kept as a reference of all possible environment variables.
# Each service now has its own .env.development file with service-specific defaults:
#   - backend_database/.env.development
#   - backend_gamelogic/.env.development
#   - live-chat/.env.development
#   - frontend/.env.development
#
# For Docker Compose production mode, use .env (created from .env.example)
#
# Usage:
# - Per-Service Dev: Each service loads its own <service>/.env.development
# - Docker Compose: Uses root .env (not this file)
# =============================================================================

# =============================================================================
# Global Application Configuration
# =============================================================================
APP_NAME=ft_transcendence
NODE_ENV=development

# =============================================================================
# Public-Facing URL Configuration
# =============================================================================
# For local development without NGINX proxy
PUBLIC_HOST=localhost
PUBLIC_PORT=
PUBLIC_PROTOCOL=http

# CORS allowed origins (allow both direct service access and potential proxy)
CORS_ORIGINS=http://localhost:4200,http://localhost:3000,http://localhost:8080,https://localhost:8443

# =============================================================================
# Backend Database Service (Local/DevContainer)
# =============================================================================
PORT=3000
HOST=::
DATABASE_PATH=./src/database/database.db

# Route prefixes
AUTH_PREFIX=/auth
OAUTH_PREFIX=/api/oauth
API_PREFIX=/api

# JWT Configuration (dev secrets - DO NOT use in production!)
JWT_ISSUER=ping-pong-api
JWT_AUDIENCE=ping-pong-clients
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# OAuth Configuration (optional for local dev)
OAUTH_STATE_SECRET=dev-oauth-state-secret-change-me
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=

# =============================================================================
# Game Logic Service (Local/DevContainer)
# =============================================================================
GAMELOGIC_PORT=3001
GAMELOGIC_HOST=::

# Game Settings
GAME_WIDTH=4000
GAME_HEIGHT=2000
MAX_SCORE=10
BALL_RADIUS=40
BALL_SPEED=40
PADDLE_SPEED=40
PHYSICS_FPS=60
RENDER_FPS=30

# =============================================================================
# Live Chat Service (Local/DevContainer)
# =============================================================================
LIVECHAT_PORT=3002
LIVECHAT_HOST=::
# For local dev: backend_database must be running on localhost:3000
AUTH_SERVICE_URL=http://localhost:3000

# =============================================================================
# Frontend Service (Local/DevContainer)
# =============================================================================
FRONTEND_PORT=4200
# For local dev: services run on localhost
PUBLIC_API_URL=http://localhost:3000
PUBLIC_WS_URL=ws://localhost:3001/ws

# =============================================================================
# Logging
# =============================================================================
LOG_LEVEL=debug

# =============================================================================
# TLS/SSL Configuration (not used in local dev)
# =============================================================================
TLS_CERT_PATH=./.certs/cert.pem
TLS_KEY_PATH=./.certs/key.pem
TLS_CA_PATH=./.certs/rootCA.pem
